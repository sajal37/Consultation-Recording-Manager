# Server configuration (all optional — sensible defaults applied)
PORT=4000

# Transcription provider: "mock" (offline, default) or "openai"
TRANSCRIPTION_PROVIDER=mock

# Required only when TRANSCRIPTION_PROVIDER=openai
# OPENAI_API_KEY=sk-...
